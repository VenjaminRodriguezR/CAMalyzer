"""Poisson reconstruction placeholder."""

import numpy as np


def poisson_reconstruct(points: np.ndarray) -> np.ndarray:
    """Return the input points unchanged as a placeholder mesh."""
    return np.asarray(points)
